import { StyleSheet } from 'react-native';
//import { Colors } from '../styles/AppStyles';  // Importez les couleurs

//const { width } = Dimensions.get('window');

const styles = StyleSheet.create({
});

export default styles;
